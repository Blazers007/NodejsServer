/**
 * Created by liang on 2015/6/4.
 */
function bringToFront(conversation) {
    /* 隐藏全部 */
    $('chat-display').children().hide();
    $('chat-display').children().each(function(index) {
        /* 遍历记录区域的所有子div */
    });
}